<mvxviews:MvxWindowsPage
    x:Class="MediaExplorer.Uwp.Views.ProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaExplorer.Uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvxviews="using:MvvmCross.Platforms.Uap.Views"
    xmlns:viewmodels="using:MediaExplorer.Core.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView ItemsSource="{Binding ViewModels}">
            <ListView.Resources>
                <DataTemplate x:Key="VirtualFolderTemplate" x:DataType="viewmodels:VirtualFolderViewModel">
                    <local:VirtualFolderView DataContext="{Binding .}"/>
                </DataTemplate>
                <DataTemplate x:Key="VirtualAlbumTemplate" x:DataType="viewmodels:VirtualAlbumFileViewModel">
                    <local:VirtualAlbumFileView DataContext="{Binding .}"/>
                </DataTemplate>
                </ListView.Resources>
        </ListView>
    </Grid>
</mvxviews:MvxWindowsPage>
